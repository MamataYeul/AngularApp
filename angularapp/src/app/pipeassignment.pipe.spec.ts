import { PipeassignmentPipe } from './pipeassignment.pipe';

describe('PipeassignmentPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeassignmentPipe();
    expect(pipe).toBeTruthy();
  });
});
